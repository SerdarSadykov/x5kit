import {Calendar} from './index';
import {useEffect} from 'react';

export const Button = () => {
  return (
    <div id="calendar">
      <Calendar
        onChange={() => {}}
      />
    </div>
  );
};
